version: "3"

services:
  nginx:
    build: ./nginx/
    ports:
      - "9000:9000"
    restart: on-failure

  elasticsearch:
    build: ./elasticsearch/
    restart: on-failure

  kibana:
    build: ./kibana/
    restart: on-failure

volumes:
  elasticsearch-volume:
  kibana-volume:
